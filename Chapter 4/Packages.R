#Not much in this chapter, but we will look at few things related to packages
(.packages())
#This shows the currently loaded packages, including the default ones
#The below command will show the list of available packages
(.packages(all.available=TRUE))
#Thats all here, not getting into much details of installing a library etc.